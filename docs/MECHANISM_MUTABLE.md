# Mechanism-Mutable DUCK Contract

This condition is specified but not yet enabled. The current configuration intentionally contains an empty mutable path list.

Activation requires completion of founder characterization and Surface-Mutable exploit analysis.

When activated, subject-facing inputs must pass through a fixed laboratory-controlled boundary. Candidate code may not rewrite scenario text, speaker identity, timing instructions, or other incoming experimental data before DUCK receives them unless the mechanism being tested is itself an explicitly designated organism-side perceptual mechanism and the raw laboratory event remains immutable.

Externally expressed output must pass through a fixed laboratory-controlled renderer. Candidate code may not rewrite the final response after rendering. Renderer code is outside the mutable set. During early mechanistic evolution, naturalness is diagnostic only and does not contribute to the optimization objective.

The scenario runner, evaluator, fixed renderer, experiment records, and sealed transfer suites are laboratory-owned and immutable. Candidate execution must remain isolated. Generated code may access only its designated mutable organism code, assigned persistent state, and explicit interaction interface.

No multi-file DUCK mutation is enabled by this document. The mutable path list remains empty until the control condition has been empirically characterized.
